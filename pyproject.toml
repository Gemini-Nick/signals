[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chan-ai-signals"
version = "0.1.0"
description = "Chan-theory signal and evidence engine for the chan.AI trading framework"
readme = "README.md"
requires-python = ">=3.11"
dynamic = ["dependencies"]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]

[project.scripts]
chan-ai-signals = "run:main"
longclaw-signals = "run:main"

[tool.setuptools]
py-modules = ["config", "run"]

[tool.setuptools.packages.find]
include = ["signals", "signals.*"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }
